diff --git a/lib/client.js b/lib/client.js
index 9f92ac8..46887a7 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -32,6 +32,14 @@ window.__ModuleLoader__.load({
 			presetMinimalDescription: "Two-tool coding agent with persistent bash and str_replace_editor.",
 			presetCordisName: "Creator mode",
 			presetCordisDescription: "Built for creating custom agent presets, with all Standard mode capabilities plus runtime inspection, plugin experiments, and preset-authoring guidance.",
+			presetAnchoredStandardName: "Anchored Standard (Progressive)",
+			presetAnchoredStandardDescription: "Dedicated chain-of-thought for DeepSeek V4 Pro, with tools unlocked progressively.",
+			presetCordisWithProductsName: "Creator Mode (with Product Subagents)",
+			presetCordisWithProductsDescription: "All of Creator mode, plus Codex and Claude Code product subagents for handing complex tasks to external coding agents.",
+			presetRouterSpecName: "Router Spec",
+			presetRouterSpecDescription: "Deep analysis and a spec draft before action — ideal for fixes, debugging, and refactors; restores the full Standard toolset after the first tool call.",
+			presetRouterStandardName: "Router Standard",
+			presetRouterStandardDescription: "Automatically picks an execution-first or analysis-first opening based on the task, then restores the full Standard toolset after the first tool call.",
 			duplicate: "Duplicate",
 			duplicateUnavailable: "This deployment has no writable preset directory",
 			delete: "Delete",
@@ -88,6 +96,14 @@ window.__ModuleLoader__.load({
 			presetMinimalDescription: "仅提供持久 bash 与 str_replace_editor 的双工具编码 Agent。",
 			presetCordisName: "创造模式",
 			presetCordisDescription: "用于创建自定义 Agent preset：具备标准模式的全部能力，并提供运行时检查、插件实验和 preset 创作指导。",
+			presetAnchoredStandardName: "渐进式标准模式",
+			presetAnchoredStandardDescription: "专为 DeepSeek V4 Pro 提供思维链，并逐步开放工具的模式",
+			presetCordisWithProductsName: "创造模式（含产品子代理）",
+			presetCordisWithProductsDescription: "具备创造模式的全部能力，并启用 Codex 与 Claude Code 产品子代理，可将复杂任务编排给外部编码 Agent 执行。",
+			presetRouterSpecName: "路由深度思考模式",
+			presetRouterSpecDescription: "先深入分析并梳理方案，再动手执行，适合修复、排查、重构等需要先理解问题的任务；首次调用工具后恢复完整标准工具集。",
+			presetRouterStandardName: "路由标准模式",
+			presetRouterStandardDescription: "根据任务自动选择「先执行」或「先分析」的首轮策略；首次调用工具后恢复完整标准工具集。",
 			duplicate: "复制",
 			duplicateUnavailable: "此部署未配置可写的预设目录",
 			delete: "删除",
@@ -141,13 +157,37 @@ window.__ModuleLoader__.load({
 			}
 		};
 		/**
+		* Locale-driven display keys for the app-managed custom presets (user root,
+		* installed by the DeepSeek Harness Code desktop app). Their picker copy is
+		* translated through the same dictionaries as the shipped presets, so the
+		* active Web locale decides the language instead of the file metadata.
+		*/
+		const MANAGED_PRESET_KEYS = {
+			"anchored-standard": {
+				name: "presetAnchoredStandardName",
+				description: "presetAnchoredStandardDescription"
+			},
+			"cordis-with-products": {
+				name: "presetCordisWithProductsName",
+				description: "presetCordisWithProductsDescription"
+			},
+			"router-spec": {
+				name: "presetRouterSpecName",
+				description: "presetRouterSpecDescription"
+			},
+			"router-standard": {
+				name: "presetRouterStandardName",
+				description: "presetRouterStandardDescription"
+			}
+		};
+		/**
 		* Resolve preset display copy without making user-authored metadata translatable.
 		* @param preset - roster row whose copy is being rendered.
 		* @param t - active Web locale lookup.
 		* @returns localized copy for a known shipped preset, otherwise file metadata.
 		*/
 		function presetDisplayText(preset, t) {
-			const keys = preset.trust === "system" ? BUILT_IN_PRESET_KEYS[preset.id] : void 0;
+			const keys = (preset.trust === "system" ? BUILT_IN_PRESET_KEYS[preset.id] : void 0) ?? MANAGED_PRESET_KEYS[preset.id];
 			if (keys !== void 0) return {
 				name: t(keys.name),
 				description: t(keys.description)
