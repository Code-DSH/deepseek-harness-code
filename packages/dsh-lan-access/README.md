# dsh-lan-access

Official-format, client-only Web plugin for the desktop app's General settings.
It exposes an explicit LAN enable/disable control and delegates copying the
private access link to the desktop bridge without rendering it in the Web UI.
