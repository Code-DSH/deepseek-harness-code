# dsh-ui-motion bundle layer: one loader row (bare package name so the
# client-modules registry qualifies the entry and serves lib/client.js).
- insert:
    - id: dsh-ui-motion
      name: 'dsh-ui-motion'
      config: {}
