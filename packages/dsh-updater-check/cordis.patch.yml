# dsh-updater-check bundle layer: one loader row (bare package name so the
# client-modules registry qualifies the entry and serves lib/client.js).
- insert:
    - id: dsh-updater-check
      name: "dsh-updater-check"
      config: {}
