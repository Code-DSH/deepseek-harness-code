name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Code structure gates: types, lint, formatting, documentation links, and
  # the security contract. Required before any commit is considered clean.
  structure:
    name: Structure (typecheck / lint / format / docs / security)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build:icon
      - run: pnpm check

  # Code compliance: dependency vulnerability audit, runtime closure of the
  # packaged surface, and the license notices artifact.
  compliance:
    name: Compliance (audit / runtime closure / notices)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Dependency vulnerability audit
        run: pnpm audit --prod
      - name: Build distribution artifacts
        run: pnpm build
      - name: Runtime closure check
        run: node scripts/check-runtime-closure.mjs
      - name: License notices present
        run: test -s build/THIRD-PARTY-NOTICES.md

  # Full test suite on every supported OS (unit, anchored, plugin, package
  # contract, and browser E2E).
  test:
    name: Test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/build/playwright
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Restore Playwright browser cache
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.PLAYWRIGHT_BROWSERS_PATH }}
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - name: Build distribution artifacts
        # The unit closure contract and the packaged runtime suites exercise
        # generated outputs (dist/**, build/routing-suite), so tests run only
        # after a clean build on every supported OS.
        run: pnpm build
      - name: Install Playwright Chromium (with Linux system dependencies)
        if: runner.os == 'Linux'
        run: pnpm exec playwright install --with-deps chromium
      - name: Install Playwright Chromium
        if: runner.os != 'Linux' && steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm exec playwright install chromium
      - run: pnpm test

  # Memory gate: the unit suites must pass under a bounded V8 heap, and the
  # peak resident set must stay below the documented tripwire.
  memory:
    name: Memory bounds
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm check:memory

  # Platform packaging adaptation: build, verify runtime closure, and produce
  # a directory package on every supported OS.
  adapt:
    name: Packaging adaptation (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: linux
            os: ubuntu-latest
            command: pnpm exec electron-builder --linux --dir
          - label: windows
            os: windows-latest
            command: pnpm exec electron-builder --win --dir
          - label: macos
            os: macos-latest
            command: pnpm exec electron-builder --mac --dir
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: node scripts/check-runtime-closure.mjs
      - run: ${{ matrix.command }}
